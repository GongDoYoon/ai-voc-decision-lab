# Optional server-managed Responses API connection.
# The deployed demo also accepts a personal OpenAI key per request and never stores it.
LLM_API_URL=https://api.openai.com/v1/responses
LLM_API_KEY=
LLM_MODEL=gpt-5.4-mini
